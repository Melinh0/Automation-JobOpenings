# Automation-JobOpenings

pip install uv

uv init

uv add playwright beautifulsoup4 requests pdfplumber python-dotenv psutil

uv run playwright install

cp .env.example .env

uv run python bot_gupy.py #or uv run python bot_gupy.py --debug